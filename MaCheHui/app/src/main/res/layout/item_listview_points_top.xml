<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#ffffff"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/jifen_turntable_iv_points"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/jifen_points"
        android:maxHeight="107dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#ffffff"
        android:gravity="center" >

        <TextView
            android:id="@+id/jifen_num_tv_points"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="77dp"
            android:drawableLeft="@drawable/my_jifen_points"
            android:drawablePadding="2dp"
            android:paddingBottom="16dp"
            android:paddingTop="16dp"
            android:text="当前积分" />

        <TextView
            android:id="@+id/history_tv_points"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/item_no_line_selector"
            android:drawableLeft="@drawable/bug_history_points"
            android:drawablePadding="2dp"
            android:paddingBottom="16dp"
            android:paddingTop="16dp"
            android:text="兑换记录" />
    </LinearLayout>

</LinearLayout>